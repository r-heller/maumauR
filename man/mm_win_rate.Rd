% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{mm_win_rate}
\alias{mm_win_rate}
\title{Win rate by seat}
\usage{
mm_win_rate(sims, call = rlang::caller_env())
}
\arguments{
\item{sims}{A tibble from \code{\link[=mm_simulate]{mm_simulate()}}.}

\item{call}{The environment used for error reporting. Experts only.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per seat within each player
count, and columns
\describe{
\item{\code{n_players}}{Seats at the table (integer).}
\item{\code{seat}}{Seat number, 1 is the first to move (integer).}
\item{\code{strategy}}{Strategy that seat played (character).}
\item{\code{wins}}{Games won (integer).}
\item{\code{games}}{Games simulated at that table size (integer).}
\item{\code{win_rate}}{Share of decided games won, summing to 1 within a
player count (double); \code{NA} when no game was decided.}
}
}
\description{
Counts wins per seat and turns them into the share of decided games each
seat took. Seats that never won still appear, so the table always has one
row per seat and player count.
}
\examples{
sims <- mm_simulate(n = 30, n_players = 3, seed = 1)
mm_win_rate(sims)

}
\seealso{
\code{\link[=mm_plot_win_rate]{mm_plot_win_rate()}} for the matching chart.

Other simulation:
\code{\link[=mm_draw_summary]{mm_draw_summary()}},
\code{\link[=mm_game_length]{mm_game_length()}},
\code{\link[=mm_simulate]{mm_simulate()}}
}
\concept{simulation}
