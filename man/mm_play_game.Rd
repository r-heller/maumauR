% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mm_play_game}
\alias{mm_play_game}
\title{Play one complete game of Mau Mau}
\usage{
mm_play_game(
  n_players = 3L,
  strategies = mm_strategy_greedy(),
  spec = mm_deck_spec(),
  hand_size = 5L,
  max_turns = 500L,
  announce_penalty = FALSE,
  trace = FALSE,
  seed = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{n_players}{Integer; number of seats. The box calls for 2 to 4.}

\item{strategies}{An \code{mm_strategy} object, or a list of them with one entry
per seat. A single strategy is recycled across all seats. See
\code{\link[=mm_strategy_greedy]{mm_strategy_greedy()}}.}

\item{spec}{A deck specification from \code{\link[=mm_deck_spec]{mm_deck_spec()}}.}

\item{hand_size}{Integer; cards dealt to each seat. The printed rules deal
five.}

\item{max_turns}{Integer; the turn cap that stops a game which cannot
finish. Reaching it warns and returns a game with no winner.}

\item{announce_penalty}{If \code{TRUE}, a seat that plays down to one card
without announcing "Mau" draws two penalty cards. Artificial strategies
always announce, so this only bites interactive players. Defaults to
\code{FALSE}.}

\item{trace}{If \code{TRUE}, record one row per step (turn, actor, action, and
the card counts of every pile) for auditing. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed. The caller's random stream is restored
afterwards.}

\item{call}{The environment used for error reporting. Experts only.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns
\describe{
\item{\code{winner}}{Winning seat (integer), \code{NA} if the game did not finish.}
\item{\code{turns}}{Turns played (integer).}
\item{\code{draws}}{Cards drawn by all seats together (integer).}
\item{\code{n_players}}{Seats at the table (integer).}
\item{\code{hand_sizes}}{List column; per-seat cards left at the end
(integer).}
\item{\code{strategy}}{List column; per-seat strategy names (character).}
\item{\code{trace}}{List column; the audit trail as a tibble with columns
\code{turn}, \code{player}, \code{action}, \code{hand_cards}, \code{draw_pile}, \code{discard_pile},
and \code{total}. 0 rows unless \code{trace = TRUE}.}
}
}
\description{
Deals a game and plays it to the end with artificial opponents, returning a
one-row record of the outcome.
}
\examples{
mm_play_game(n_players = 3, seed = 1)

# The audit trail lets you check that no card is lost or duplicated.
one <- mm_play_game(n_players = 2, seed = 7, trace = TRUE)
unique(one$trace[[1]]$total)

}
\seealso{
\code{\link[=mm_simulate]{mm_simulate()}} to repeat this many times, \code{\link[=mm_new_game]{mm_new_game()}} and
\code{\link[=mm_step]{mm_step()}} for turn-by-turn control.

Other engine:
\code{\link[=mm_hand]{mm_hand()}},
\code{\link[=mm_new_game]{mm_new_game()}},
\code{\link[=mm_step]{mm_step()}}
}
\concept{engine}
