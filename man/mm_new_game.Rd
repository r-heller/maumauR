% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mm_new_game}
\alias{mm_new_game}
\alias{print.mm_game}
\title{Start a game of Mau Mau}
\usage{
mm_new_game(
  n_players = 3L,
  strategies = mm_strategy_greedy(),
  spec = mm_deck_spec(),
  hand_size = 5L,
  announce_penalty = FALSE,
  trace = FALSE,
  seed = NULL,
  call = rlang::caller_env()
)

\method{print}{mm_game}(x, ...)
}
\arguments{
\item{n_players}{Integer; number of seats. The box calls for 2 to 4.}

\item{strategies}{An \code{mm_strategy} object, or a list of them with one entry
per seat. A single strategy is recycled across all seats. See
\code{\link[=mm_strategy_greedy]{mm_strategy_greedy()}}.}

\item{spec}{A deck specification from \code{\link[=mm_deck_spec]{mm_deck_spec()}}.}

\item{hand_size}{Integer; cards dealt to each seat. The printed rules deal
five.}

\item{announce_penalty}{If \code{TRUE}, a seat that plays down to one card
without announcing "Mau" draws two penalty cards. Artificial strategies
always announce, so this only bites interactive players. Defaults to
\code{FALSE}.}

\item{trace}{If \code{TRUE}, record one row per step (turn, actor, action, and
the card counts of every pile) for auditing. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed. The caller's random stream is restored
afterwards.}

\item{call}{The environment used for error reporting. Experts only.}

\item{x}{An object of class \code{mm_game}.}

\item{...}{Not used.}
}
\value{
An object of class \code{mm_game}: a list holding the deck, the hands,
the draw and discard piles, the colour in force, the seat to move, and the
running turn, draw, and winner counters.
}
\description{
Shuffles a deck, deals a hand to every seat, and turns the first card face
up. The returned object is the complete game state, open to inspection:
advance it one action at a time with \code{\link[=mm_step]{mm_step()}}, or play it out in one call
with \code{\link[=mm_play_game]{mm_play_game()}}.
}
\details{
The opening card's effect is not applied: only its colour (and value) set
the state, so the first seat always moves. If the opening card is a wild,
the colour in force is drawn at random from the deck colours.
}
\examples{
game <- mm_new_game(n_players = 3, seed = 1)
game
mm_hand(game, player = 1)

}
\seealso{
\code{\link[=mm_step]{mm_step()}} to advance the game, \code{\link[=mm_hand]{mm_hand()}} and \code{\link[=mm_top_card]{mm_top_card()}}
to inspect it.

Other engine:
\code{\link[=mm_hand]{mm_hand()}},
\code{\link[=mm_play_game]{mm_play_game()}},
\code{\link[=mm_step]{mm_step()}}
}
\concept{engine}
