% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deck.R
\name{mm_deck_spec}
\alias{mm_deck_spec}
\alias{print.mm_deck_spec}
\title{Deck specification for "der kleine ICE" Mau Mau}
\usage{
mm_deck_spec(
  colours = c("red", "green", "blue", "yellow"),
  numbers = 1:5,
  plus2 = 1L,
  skip = 1L,
  wild = 4L,
  call = rlang::caller_env()
)

\method{print}{mm_deck_spec}(x, ...)
}
\arguments{
\item{colours}{Character vector of colour names. Must be unique and
non-missing.}

\item{numbers}{Integer vector of number-card face values present in each
colour. Must be unique and non-missing.}

\item{plus2}{Integer; number of \code{+2} (draw two) cards per colour.}

\item{skip}{Integer; number of skip (\code{Aussetzen}) cards per colour.}

\item{wild}{Integer; number of colourless wild (\code{Farbwahl}) cards.}

\item{call}{The environment used for error reporting. Experts only.}

\item{x}{An object of class \code{mm_deck_spec}.}

\item{...}{Not used.}
}
\value{
An object of class \code{mm_deck_spec}: a list with elements
\describe{
\item{\code{colours}}{Colour names (character).}
\item{\code{numbers}}{Number faces per colour (integer).}
\item{\code{plus2}}{\code{+2} cards per colour (integer, length 1).}
\item{\code{skip}}{Skip cards per colour (integer, length 1).}
\item{\code{wild}}{Wild cards in the deck (integer, length 1).}
\item{\code{size}}{Total number of cards (integer, length 1).}
}
}
\description{
Builds the specification of the DB "der kleine ICE" Mau Mau deck: which
colours exist, which number faces each colour carries, and how many special
cards the deck holds. The specification is the single source of truth for
deck composition - the engine, the simulator, the summaries, and the Shiny
app all read the deck from it.
}
\details{
\strong{This is the single point of correction for the deck.} The box states 32
cards (\verb{Inhalt: 32 Spielkarten}). The defaults are the only reconstruction
that reaches that total while keeping every card type described in the
printed rules:

\itemize{
\item four colours (red, green, blue, yellow),
\item five number faces per colour (\code{1:5}) - 20 cards,
\item one \code{+2} (draw two) per colour - 4 cards,
\item one skip (\code{Aussetzen}) per colour - 4 cards,
\item four colourless wild cards (\code{Farbwahl}) - 4 cards.
}

The \emph{number of number faces} (five per colour) is fixed by arithmetic once
the specials are known; the face values themselves (\code{1:5}) remain a
reconstruction pending a physical card count. Adjust \code{numbers}, \code{plus2},
\code{skip}, and \code{wild} here and everything downstream follows. A specification
whose total is not 32 is accepted but warns, so a wrongly built deck can
never pass unnoticed.
}
\examples{
spec <- mm_deck_spec()
spec
spec$size

# A specification that departs from the printed 32-card total warns:
suppressWarnings(mm_deck_spec(numbers = 0:5))$size

}
\seealso{
\code{\link[=mm_build_deck]{mm_build_deck()}} to turn a specification into a card table.

Other deck:
\code{\link[=mm_build_deck]{mm_build_deck()}}
}
\concept{deck}
