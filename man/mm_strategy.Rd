% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategies.R
\name{mm_strategy}
\alias{mm_strategy}
\alias{mm_strategy_greedy}
\alias{mm_strategy_random}
\alias{mm_strategy_hold_wilds}
\alias{print.mm_strategy}
\title{Artificial strategies}
\usage{
mm_strategy_greedy()

mm_strategy_random()

mm_strategy_hold_wilds()

\method{print}{mm_strategy}(x, ...)
}
\arguments{
\item{x}{An object of class \code{mm_strategy}.}

\item{...}{Not used.}
}
\value{
The constructors return a function of class \code{mm_strategy} with
signature \verb{(hand, state)}, returning a single integer card position or
\code{NA_integer_}. \code{print()} returns its input invisibly.
}
\description{
Strategies decide which card a seat plays. Each constructor returns a
function of class \code{mm_strategy} that the engine calls once per decision.
}
\details{
A strategy is a function \verb{(hand, state)} returning the position of the card
to play (an index into the rows of \code{hand}), or \code{NA_integer_} to draw and
pass. It must only ever return a position listed in \code{state$legal}.

\code{hand} is a \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{card} (integer card id),
\code{color}, \code{value}, and \code{type}. \code{state} is a list with elements
\describe{
\item{\code{player}}{Seat to move (integer).}
\item{\code{n_players}}{Number of seats (integer).}
\item{\code{turn}}{Completed turns so far (integer).}
\item{\code{legal}}{Playable positions in \code{hand} (integer, possibly empty).}
\item{\code{top}}{The discard pile's top card (one-row tibble).}
\item{\code{active_color}}{The colour currently in force (character).}
\item{\code{pending_draw}}{Cards stacked by unanswered \code{+2}s (integer).}
\item{\code{hand_sizes}}{Cards held by each seat (integer).}
\item{\code{draw_remaining}}{Cards left in the draw pile (integer).}
\item{\code{discard_size}}{Cards on the discard pile (integer).}
\item{\code{colours}}{Deck colours (character).}
}

Each strategy also carries a colour picker used when it plays a wild:
\code{mm_strategy_greedy()} and \code{mm_strategy_hold_wilds()} demand the colour they
hold most of, \code{mm_strategy_random()} demands a uniformly random colour.

The three supplied strategies differ only in which legal card they pick:
\describe{
\item{\code{mm_strategy_greedy()}}{The first legal card in hand order.}
\item{\code{mm_strategy_random()}}{A uniformly random legal card.}
\item{\code{mm_strategy_hold_wilds()}}{A non-wild legal card when one exists,
keeping wild cards for turns that would otherwise be a draw.}
}
}
\examples{
greedy <- mm_strategy_greedy()
greedy

# Strategies are plain functions; the engine calls them per decision.
hand <- mm_build_deck()[1:3, ]
hand$card <- 1:3
state <- list(legal = c(1L, 3L), colours = c("red", "green", "blue",
                                             "yellow"))
greedy(hand, state)

}
\seealso{
\code{\link[=mm_play_game]{mm_play_game()}} and \code{\link[=mm_simulate]{mm_simulate()}}, which take strategies per
seat.
}
\concept{strategies}
