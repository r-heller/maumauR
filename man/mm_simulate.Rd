% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{mm_simulate}
\alias{mm_simulate}
\title{Simulate many games of Mau Mau}
\usage{
mm_simulate(
  n = 100L,
  n_players = 3L,
  strategies = mm_strategy_greedy(),
  spec = mm_deck_spec(),
  hand_size = 5L,
  max_turns = 500L,
  announce_penalty = FALSE,
  trace = FALSE,
  seed = NULL,
  verbose = FALSE,
  call = rlang::caller_env()
)
}
\arguments{
\item{n}{Integer; number of games to simulate.}

\item{n_players}{Integer; number of seats. The box calls for 2 to 4.}

\item{strategies}{An \code{mm_strategy} object, or a list of them with one entry
per seat. A single strategy is recycled across all seats. See
\code{\link[=mm_strategy_greedy]{mm_strategy_greedy()}}.}

\item{spec}{A deck specification from \code{\link[=mm_deck_spec]{mm_deck_spec()}}.}

\item{hand_size}{Integer; cards dealt to each seat. The printed rules deal
five.}

\item{max_turns}{Integer; the turn cap that stops a game which cannot
finish. Reaching it warns and returns a game with no winner.}

\item{announce_penalty}{If \code{TRUE}, a seat that plays down to one card
without announcing "Mau" draws two penalty cards. Artificial strategies
always announce, so this only bites interactive players. Defaults to
\code{FALSE}.}

\item{trace}{If \code{TRUE}, record one row per step (turn, actor, action, and
the card counts of every pile) for auditing. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed. The caller's random stream is restored
afterwards.}

\item{verbose}{If \code{TRUE}, show a progress bar. Defaults to \code{FALSE}.}

\item{call}{The environment used for error reporting. Experts only.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with \code{n} rows: the columns of
\code{\link[=mm_play_game]{mm_play_game()}} (\code{winner}, \code{turns}, \code{draws}, \code{n_players}, \code{hand_sizes},
\code{strategy}, \code{trace}) preceded by \code{game}, an integer game id. \code{n = 0}
yields a 0-row tibble with those columns.
}
\description{
Runs \code{n} independent games under the same table setup and returns one tidy
row per game. This is the entry point for win-rate, game-length, and
draw-count analysis.
}
\examples{
sims <- mm_simulate(n = 20, n_players = 3, seed = 1)
sims

# One strategy per seat.
mixed <- mm_simulate(
  n = 20,
  n_players = 3,
  strategies = list(
    mm_strategy_greedy(),
    mm_strategy_random(),
    mm_strategy_hold_wilds()
  ),
  seed = 2
)
mm_win_rate(mixed)

}
\seealso{
\code{\link[=mm_win_rate]{mm_win_rate()}}, \code{\link[=mm_game_length]{mm_game_length()}}, and \code{\link[=mm_draw_summary]{mm_draw_summary()}} for
the summaries built on this table.

Other simulation:
\code{\link[=mm_draw_summary]{mm_draw_summary()}},
\code{\link[=mm_game_length]{mm_game_length()}},
\code{\link[=mm_win_rate]{mm_win_rate()}}
}
\concept{simulation}
