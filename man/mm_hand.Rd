% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mm_hand}
\alias{mm_hand}
\alias{mm_legal_moves}
\alias{mm_top_card}
\title{Inspect a game in progress}
\usage{
mm_hand(game, player = game$current, call = rlang::caller_env())

mm_legal_moves(game, player = game$current, call = rlang::caller_env())

mm_top_card(game, call = rlang::caller_env())
}
\arguments{
\item{game}{An \code{mm_game} object from \code{\link[=mm_new_game]{mm_new_game()}}.}

\item{player}{Integer; the seat to inspect. Defaults to the seat to move.}

\item{call}{The environment used for error reporting. Experts only.}
}
\value{
\code{mm_hand()} returns a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per held card and
columns \code{card} (integer card id), \code{color} (character), \code{value} (integer),
\code{type} (character), and \code{legal} (logical, whether the card could be played
on the current discard). A seat with no cards yields a 0-row tibble.

\code{mm_legal_moves()} returns an integer vector of row positions into
\code{mm_hand()}, empty when nothing is playable - never \code{NULL}.

\code{mm_top_card()} returns a one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{color},
\code{value}, \code{type}, and \code{active_color} (the colour in force, which differs
from \code{color} after a wild).
}
\description{
Accessors for the pieces of an \code{mm_game} a player or an interface needs:
the cards a seat holds, the positions it may legally play, and the card on
top of the discard pile.
}
\examples{
game <- mm_new_game(n_players = 3, seed = 42)
mm_top_card(game)
mm_hand(game)
mm_legal_moves(game)

}
\seealso{
\code{\link[=mm_step]{mm_step()}} to play one of the legal moves.

Other engine:
\code{\link[=mm_new_game]{mm_new_game()}},
\code{\link[=mm_play_game]{mm_play_game()}},
\code{\link[=mm_step]{mm_step()}}
}
\concept{engine}
