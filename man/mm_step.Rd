% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mm_step}
\alias{mm_step}
\title{Advance a game by one action}
\usage{
mm_step(
  game,
  card = NULL,
  color = NULL,
  announce = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{game}{An \code{mm_game} object from \code{\link[=mm_new_game]{mm_new_game()}}.}

\item{card}{Position of the card to play within \code{\link[=mm_hand]{mm_hand()}}, \code{NA} to draw
or pass, or \code{NULL} (the default) to let the seat's strategy choose.}

\item{color}{Colour to demand when playing a wild. \code{NULL} (the default)
asks the seat's strategy.}

\item{announce}{Set to \code{FALSE} to omit the "Mau" call when a play leaves one
card in hand. Only matters when the game was created with
\code{announce_penalty = TRUE}.}

\item{call}{The environment used for error reporting. Experts only.}
}
\value{
The updated \code{mm_game} object.
}
\description{
Plays a single action for the seat to move: either play a card, or draw.
Leave \code{card} at \code{NULL} to let the seat's strategy decide, which is what
\code{\link[=mm_play_game]{mm_play_game()}} does; pass a position to play a specific card, which is
what an interactive front end does.
}
\details{
A turn is at most two actions. A seat with no legal card draws one; if the
drawn card is playable the seat may still play it, otherwise the turn ends
immediately. A seat that has already drawn and does not play passes.

\code{+2} cards stack: the penalty accumulates while seats keep answering with a
\code{+2} of their own, and the first seat that cannot (or will not) answer draws
the whole pile and loses its turn. While a penalty is pending, \code{+2} cards
are the only legal play - a wild does not cancel it.
}
\examples{
game <- mm_new_game(n_players = 3, seed = 42)
game <- mm_step(game)
mm_top_card(game)

# Play a specific card interactively.
legal <- mm_legal_moves(game)
if (length(legal) > 0) {
  game <- mm_step(game, card = legal[[1]], color = "red")
}

}
\seealso{
\code{\link[=mm_play_game]{mm_play_game()}} to run a whole game, \code{\link[=mm_legal_moves]{mm_legal_moves()}} to see
what is playable.

Other engine:
\code{\link[=mm_hand]{mm_hand()}},
\code{\link[=mm_new_game]{mm_new_game()}},
\code{\link[=mm_play_game]{mm_play_game()}}
}
\concept{engine}
